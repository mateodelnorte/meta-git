#!/usr/bin/env node

const program = require('commander');
const debug = require('debug')('meta-git');

program
  .command('clone', 'clone meta and child repositories')
  .command('status', 'git status of meta and child repositories')
  .parse(process.argv);