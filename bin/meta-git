#!/usr/bin/env node

const debug = require('debug')('meta-git');
const metaLoop = require('meta-loop');

const command = `git ${process.argv.slice(2).join(' ')}`;

debug(`executing ${command}`);

metaLoop(command);
